{% extends 'base.html.twig' %}

{% block title %}Hello ProductosController!
{% endblock %}
{% block carousel %}{% endblock %}
{% block wrapper %}
	<div class="container-fluid mt-2">
		<div class="row">
			<div class="col-md-6 order-md-1">
				{# <div id="carouselImages" class="carousel slide" data-bs-ride="carousel">
					<div class="carousel-inner">

						<div class="carousel-item active">
							<img class="d-block w-100" src="<?php echo base_url(); ?>img/productos/<?= $codigo ?>/principal.jpg">
						</div>

						{# {% for imagen in imagenes %} #}
							{# <div class="carousel-item"> #}
								<img class="d-block w-100" src="{{ asset('uploads/productos/'~ producto.categoria ~'/' ~ producto.imagenes[0].nombre)}}">
							{# </div> #}
						{# {% endfor %} #}
					{# </div>
					<button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="prev">
						<span class="carousel-control-prev-icon" aria-hidden="true"></span>
						<span class="visually-hidden">Previous</span>
					</button>
					<button class="carousel-control-next" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="next">
						<span class="carousel-control-next-icon" aria-hidden="true"></span>
						<span class="visually-hidden">Next</span>
					</button>
				</div> #} 

			</div>
			<div class="col-md-6 order-md-2">
				<h2>{{producto.nombre}}</h2>
				<h4>Características: </h4>
				<p>{{producto.contenido|raw}}</p>
				<p class="lead">Precio: {{producto.precio}} €</p>
				<div class="d-grid gap-3 col-10 mx-auto">
		
					<a class="btn btn-outline-primary" type="button" href="{{ path('carrito_pagar', {'id': producto.id})}}">Comprar ahora</a>
					{% if is_granted('ROLE_USER') %}
					<a class="btn btn-outline-primary" data-bs-target="#exampleModal" type="button" href="{{ path('carrito_add', {'id': producto.id})}}">Agregar al carrito</a>
					{% endif %}
				</div>
			</div>
		</div>
	{% endblock %}
