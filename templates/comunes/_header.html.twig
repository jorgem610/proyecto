{# HEADER #}
{# <div class="container-fluid">
<header class="container-fluid shadow-sm">
	<div class="row flex-nowrap justify-content-between align-items-center p-0 m-0">
		<div class="col-2">
			<a href="{{ path ('app_inicio')}}"><img src="{{ asset('images/logo.png')}}" class="img-fluid"></a>
		</div>
		<div class="col-7">
			{% if formSearch is defined %}
			<div >
					{{ form_start(formSearch)}}
					<div class="row d-flex ">
						{{form_row(formSearch.palabras)}}
						{{form_row(formSearch.Buscar)}}
					</div>
					{{ form_end(formSearch)}}
			</div>
			{% endif %}
		</div>


			<div class="col-3 d-flex justify-content-end align-items-center">
			
			</div>

		</div>
</header>
</div> #}

  <div class="container-fluid bg-dark">
    <header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-0 ">
	{# LOGO #}
      <a href="{{ path ('app_inicio')}}"><img src="{{ asset('images/logo2.png')}}" class="img-fluid" width="150px"></a>
	  
	{# BUSQUEDAD #}
      <div class="col-12 col-md-auto mb-2 justify-content-center mb-md-0">
        {% if formSearch is defined %}
					{{ form_start(formSearch)}}
					<div class="row w-100 justify-content-center">
						{{form_row(formSearch.palabras)}}
						{{form_row(formSearch.Buscar)}}
					</div>
					{{ form_end(formSearch)}}
			
			{% endif %}
      </div>

      <div class="col-md-3 text-end">
        	<a class="btn btn-primary me-2" href="{{ path('app_login') }}"><i class="bi bi-person"></i></a>
			<a class="btn btn-primary me-2" href="{{ path('app_login') }}"><i class="bi bi-cart"></i></a>
      </div>
    </header>
  </div>

